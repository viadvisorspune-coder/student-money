/**
 * What the app calls itself.
 *
 * One constant, so the name on a widget, a notification and the browser tab can never
 * drift apart. The design system keeps its own name in its file headers, which are
 * copied verbatim from the handoff and are not touched here.
 */
export const APP_NAME = 'Margin'
