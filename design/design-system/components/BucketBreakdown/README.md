# BucketBreakdown

A stacked bar and ranked list showing each bucket's share of spending for a period.

- **Consumer provides:** `items` (`name`, `amount`, `tone`). Buckets with zero spend are hidden automatically.
- Sort items by amount, largest first.
