# HighlightCard

A short colour banner for the one thing to act on (`sunflower`) or a gentle win or tip (`butter`).

- **Consumer provides:** `title`, an optional `subtitle`, `tone` and an optional `action` node (usually a dark IconButton).
- Use at most one `sunflower` card per screen. Keep the title to one line and the subtitle to one sentence.
