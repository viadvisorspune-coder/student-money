# TextField

A labelled text or amount input on a soft fill.

- **Consumer provides:** `label`, `value` and `onChange`, and optionally `prefix` (“₹”), `placeholder`, `hint`, `multiline` and `inputMode` (use `numeric` for amounts).
- Every field has a visible label. Don't rely on the placeholder alone.
