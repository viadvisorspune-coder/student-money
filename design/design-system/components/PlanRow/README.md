# PlanRow

One step in a plan or agenda list, with the current step filled in `coral-strong`.

- **Consumer provides:** `title`, `time`, `active` and `onClick`.
- Stack rows with a `space-1` gap on `surface`. Only one row can be active. The active fill is `coral-strong`, not `coral`, so the 18px white title passes contrast.
