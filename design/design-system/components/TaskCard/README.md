# TaskCard

A white card for a scheduled money item, such as a bill, a recurring transfer or a study-group split.

- **Consumer provides:** `title`, `time`, `subtitle`, `chips` (strings), an optional `people` node, and `onOpen`.
- Keep chips to two: when and how much. The arrow button opens the detail sheet, and it is the card’s only action.
