# ProgressCard

A sky card with one large number showing progress toward a savings goal or budget.

- **Consumer provides:** `label`, `message` (encouraging, under 25 characters), `value` (for example “78%”) and an optional `toggle` (a SegmentedToggle).
- Use a percentage or a short amount. If a value is too long for `text-metric`, move it into `message`.
