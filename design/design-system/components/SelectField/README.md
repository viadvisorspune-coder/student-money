# SelectField

A labelled dropdown, used for the “For?” question on the estimate screen and anywhere a value comes from a fixed list.

- **Consumer provides:** `label`, `options`, `value`, `onChange`, and optionally `placeholder` and `hint`.
- It is a native select, so it uses the phone's own picker. Keep option labels to two or three words.
