# DecisionInput

The “Kharcha?” card: type a planned spend and see its impact before you decide.

- **Consumer provides:** `onSubmit` (receives the raw text, which the app parses for an amount), `placeholder`, and optional `suggestions`.
- It is a sunflower card, so it is the only sunflower card on Home.
