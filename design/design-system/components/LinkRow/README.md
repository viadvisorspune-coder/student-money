# LinkRow

A full-width row that leads to another screen, like “See transaction history ↗”.

- **Consumer provides:** `title`, an optional `caption` (a live fact such as “23 this month”), `tone` and `onClick`.
- Use it for navigation only. For an action, use Button.
