# SegmentedToggle

A two- or three-option switch for changing a card’s time range (Weekly or Monthly).

- **Consumer provides:** `options`, and either `value` with `onChange` (controlled) or `defaultValue`.
- Use one-word labels in sentence case. It is not for navigation; use BottomNav for that.
