# AvatarStack

Overlapping initials avatars that show who shares a budget, a split bill or a goal.

- **Consumer provides:** `people` (`initials`, `name`, `tone`), plus an optional `max` (default 3) and `extra` count.
- The overflow bubble is `ink`, or `coral` for a highlighted group. Use `lg` on detail sheets.
