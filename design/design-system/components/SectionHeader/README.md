# SectionHeader

A small uppercase label that opens a group of cards or rows, with an optional action on the right.

- **Consumer provides:** `title`, an optional `action` node and `onCanvas` when it sits directly on `canvas`.
- Keep it to two or three words: “Set aside”, “Buckets”, “TBD”.
